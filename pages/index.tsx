import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="p-8 w-full bg-stone-50 text-stone-800 rounded-md text-center">
      This text is styled using Tailwind CSS
    </div>
  );
};

export default Home;
